<div class="container">
  <h5 class="col-md-7 mb-5 mt-5">
      <span class="py-2 px-4 bg-ligh alert-secondary">ジャンル一覧・投稿</span>
    </h5>

    <div class="row">
      <div class="col-10">
      <%= form_with url: admin_genres_path, model: @genre, local: true do |f| %>

        <table>
          <td><%= f.label :name, 'ジャンル名' %></td>
          <td><%= f.text_field :name, class:'form-control' %></td>
          <td><%= f.submit '新規登録', class:'btn btn-success' %></td>
        </table>
       <% end %>
    </div>
    </div>
    <div class="row">
      <div  class="col-5">
        <table class="table table-hover table-inverse  mt-5" >
        <thead class="thead-light">
            <th>
              ジャンル
            </th>
            <th></th>
          </thead>
          <tbody>
            <% @genres.each do |genre| %>
            <tr>
              <td>
                <%= genre.name %>
              </td>
              <td align="center">
                <%= link_to "編集する",edit_admin_genre_path(genre), class: "btn btn-success" %>
              </td>
            </tr>
            <% end %>
          </tbody>
        </table>
      </div>

    </div>
  </div>
</div>